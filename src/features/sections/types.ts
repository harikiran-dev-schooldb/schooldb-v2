export type SectionListItem = {
  id: string;

  name: string;

  classId: string;

  className: string;

  displayOrder: number;

  active: boolean;
};

export type ClassOption = {
  id: string;
  name: string;
};