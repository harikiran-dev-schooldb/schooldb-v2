export type PeriodListItem = {
  id: string;

  name: string;

  startTime: string;

  endTime: string;

  displayOrder: number;

  active: boolean;
};

export type PeriodOption = {
  id: string;

  label: string;
};