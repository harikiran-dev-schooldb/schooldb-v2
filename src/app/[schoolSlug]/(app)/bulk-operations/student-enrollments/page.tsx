"use client";

import { useMemo, useRef, useState } from "react";
import Link from "next/link";
import { ArrowLeft, CheckCircle2, Download, FileSpreadsheet, Loader2, UploadCloud, XCircle } from "lucide-react";
import { PageHeader } from "@/components/common/PageHeader";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { useSchool } from "@/contexts/school-context";

type Row = { admissionNo: string; academicYear: string; className: string; sectionName: string; rollNo: string; admissionDate: string; active: string };
type ErrorRow = { row: number; message: string };
const HEADERS = ["admissionNo", "academicYear", "className", "sectionName", "rollNo", "admissionDate", "active"] as const;
const TEMPLATE = [HEADERS.join(","), "00005,2026-27,Class 1,A,1,15/06/2026,true", "00006,2026-27,Class 1,A,2,15/06/2026,true"].join("\n");
function parseLine(line: string) { const values: string[] = []; let current = ""; let quoted = false; for (const c of line) { if (c === '"') quoted = !quoted; else if (c === "," && !quoted) { values.push(current.trim()); current = ""; } else current += c; } values.push(current.trim()); return values; }
function parseCsv(text: string) { const lines = text.replace(/^\uFEFF/, "").split(/\r?\n/).filter((line) => line.trim()); if (!lines.length) throw new Error("The file is empty."); if (parseLine(lines[0]).join("|") !== HEADERS.join("|")) throw new Error(`Invalid columns. Expected: ${HEADERS.join(", ")}`); const rows: Row[] = []; const errors: ErrorRow[] = []; lines.slice(1).forEach((line, index) => { const v = parseLine(line); const row = Object.fromEntries(HEADERS.map((h, i) => [h, v[i] ?? ""])) as Row; if (!row.admissionNo || !row.academicYear || !row.className || !row.sectionName) { errors.push({ row: index + 2, message: "Admission number, academic year, class and section are required." }); return; } if (row.rollNo && (!/^\d+$/.test(row.rollNo) || Number(row.rollNo) <= 0)) { errors.push({ row: index + 2, message: "Roll number must be a positive integer." }); return; } if (row.admissionDate && !/^\d{2}\/\d{2}\/\d{4}$/.test(row.admissionDate)) { errors.push({ row: index + 2, message: "Admission date must use DD/MM/YYYY." }); return; } if (!/^(true|false|yes|no|1|0)$/i.test(row.active || "true")) { errors.push({ row: index + 2, message: "Active must be true/false, yes/no, or 1/0." }); return; } row.active = /^(true|yes|1)$/i.test(row.active || "true") ? "true" : "false"; rows.push(row); }); return { rows, errors, totalRows: lines.length - 1 }; }
function downloadTemplate() { const blob = new Blob([TEMPLATE], { type: "text/csv;charset=utf-8" }); const url = URL.createObjectURL(blob); const a = document.createElement("a"); a.href = url; a.download = "schooldb-student-enrollment-template.csv"; a.click(); URL.revokeObjectURL(url); }
export default function BulkStudentEnrollmentsPage() { const { school } = useSchool(); const inputRef = useRef<HTMLInputElement>(null); const [fileName, setFileName] = useState(""); const [totalRows, setTotalRows] = useState(0); const [rows, setRows] = useState<Row[]>([]); const [errors, setErrors] = useState<ErrorRow[]>([]); const [message, setMessage] = useState<string | null>(null); const [importing, setImporting] = useState(false); const [result, setResult] = useState<number | null>(null); const duplicateCount = useMemo(() => { const keys = new Set<string>(); let count = 0; rows.forEach((r) => { const key = `${r.admissionNo.toLowerCase()}:${r.academicYear.toLowerCase()}`; if (keys.has(key)) count += 1; keys.add(key); }); return count; }, [rows]); async function handleFile(file: File) { setFileName(file.name); setMessage(null); setResult(null); setTotalRows(0); setRows([]); setErrors([]); if (!file.name.toLowerCase().endsWith(".csv")) { setMessage("Upload a CSV file using the Student Enrollment template."); return; } try { const parsed = parseCsv(await file.text()); setTotalRows(parsed.totalRows); setRows(parsed.rows); setErrors(parsed.errors); } catch (e) { setMessage(e instanceof Error ? e.message : "Unable to read the file."); } } async function importRows() { if (!rows.length || errors.length || duplicateCount) return; setImporting(true); setMessage(null); try { const response = await fetch("/api/v1/student-enrollments/bulk", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ enrollments: rows }) }); const payload = await response.json() as { success?: boolean; message?: string; data?: { created: number } }; if (!response.ok || !payload.success || !payload.data) throw new Error(payload.message ?? "Bulk student enrollment failed."); setResult(payload.data.created); } catch (e) { setMessage(e instanceof Error ? e.message : "Bulk student enrollment failed."); } finally { setImporting(false); } } function reset() { setFileName(""); setTotalRows(0); setRows([]); setErrors([]); setMessage(null); setResult(null); if (inputRef.current) inputRef.current.value = ""; } const hasFile = Boolean(fileName); return <div className="space-y-8 pb-12"><PageHeader eyebrow="Bulk Operations" title="Bulk Student Enrollment" description="Enroll existing students into an academic year, class, section and roll number from one CSV." action={<Button variant="outline" onClick={downloadTemplate}><Download className="size-4" />Download Template</Button>} /><div className="flex items-center gap-2 text-xs text-muted-foreground"><Link href={`/${school.slug}/bulk-operations`} className="font-semibold text-primary hover:underline">Bulk Operations</Link><span>/</span><span>Student Enrollment</span></div><Card className="premium-card overflow-hidden rounded-2xl border-0"><CardHeader className="border-b border-border/60 px-6 py-5"><div className="flex items-center gap-3"><div className="flex size-10 items-center justify-center rounded-xl bg-primary/10 text-primary"><FileSpreadsheet className="size-5" /></div><div><CardTitle>Enrollment import</CardTitle><p className="mt-1 text-xs text-muted-foreground">CSV columns: admissionNo, academicYear, className, sectionName, rollNo, admissionDate, active</p></div></div></CardHeader><CardContent className="space-y-6 p-6"><input ref={inputRef} type="file" accept=".csv,text/csv" className="hidden" onChange={(e) => { const f = e.target.files?.[0]; if (f) void handleFile(f); }} />{!hasFile && !message && <button type="button" onClick={() => inputRef.current?.click()} className="flex min-h-64 w-full flex-col items-center justify-center rounded-2xl border-2 border-dashed border-border/70 bg-muted/20 px-6 text-center transition-all hover:border-primary/40 hover:bg-primary/[0.03]"><div className="flex size-14 items-center justify-center rounded-2xl bg-primary/10 text-primary"><UploadCloud className="size-7" /></div><p className="mt-4 text-base font-bold">Upload enrollment CSV</p><p className="mt-1 max-w-md text-sm text-muted-foreground">Students must already exist. The importer only creates enrollments.</p></button>}{message && <div className="flex items-start gap-3 rounded-2xl border border-destructive/20 bg-destructive/5 p-4"><XCircle className="mt-0.5 size-5 shrink-0 text-destructive" /><div className="flex-1 text-sm"><p className="font-semibold">Import cannot continue</p><p className="mt-1 text-muted-foreground">{message}</p></div><Button size="sm" variant="outline" onClick={reset}>Reset</Button></div>}{hasFile && !message && <><div className="flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-border/60 bg-muted/20 p-4"><div><p className="text-sm font-semibold">{fileName}</p><p className="mt-1 text-xs text-muted-foreground">{totalRows} total rows detected · {rows.length} valid rows ready for review</p></div><div className="flex flex-wrap gap-2"><Badge variant={rows.length ? "success" : "destructive"}>{rows.length ? <CheckCircle2 className="size-3" /> : <XCircle className="size-3" />}{rows.length} valid</Badge>{duplicateCount > 0 && <Badge variant="destructive">{duplicateCount} duplicate student/year rows</Badge>}{errors.length > 0 && <Badge variant="destructive">{errors.length} validation errors</Badge>}</div></div>{errors.length > 0 && <div className="rounded-2xl border border-destructive/20 bg-destructive/5 p-4"><p className="text-sm font-semibold text-destructive">Fix these rows before importing</p><div className="mt-3 max-h-44 space-y-2 overflow-auto text-xs text-muted-foreground">{errors.slice(0, 50).map((e) => <p key={`${e.row}-${e.message}`}><span className="font-semibold text-foreground">Row {e.row}:</span> {e.message}</p>)}</div></div>}{rows.length > 0 && <div className="overflow-hidden rounded-2xl border border-border/60"><div className="max-h-[460px] overflow-auto"><table className="w-full text-sm"><thead className="sticky top-0 z-10 border-b border-border/60 bg-card"><tr><th className="px-4 py-3 text-left text-[10px] font-bold uppercase tracking-wider text-muted-foreground">#</th>{HEADERS.map((h) => <th key={h} className="px-4 py-3 text-left text-[10px] font-bold uppercase tracking-wider text-muted-foreground">{h}</th>)}</tr></thead><tbody>{rows.slice(0, 100).map((r, i) => <tr key={`${r.admissionNo}-${i}`} className="border-b border-border/40 last:border-0 hover:bg-muted/20"><td className="px-4 py-3 text-xs text-muted-foreground">{i + 1}</td>{HEADERS.map((h) => <td key={h} className="whitespace-nowrap px-4 py-3">{r[h]}</td>)}</tr>)}</tbody></table></div></div>}{result !== null && <div className="rounded-2xl border border-emerald-500/20 bg-emerald-500/5 p-4"><p className="text-sm font-bold">Import complete</p><p className="mt-1 text-xs text-muted-foreground">{result} student enrollments created.</p></div>}<div className="flex justify-end gap-3"><Button variant="outline" onClick={reset} disabled={importing}><ArrowLeft className="size-4" />Start Over</Button><Button onClick={() => void importRows()} disabled={importing || !!errors.length || duplicateCount > 0 || !rows.length}>{importing ? <Loader2 className="size-4 animate-spin" /> : <UploadCloud className="size-4" />}{importing ? "Importing..." : `Import ${rows.length} Enrollments`}</Button></div></>}</CardContent></Card></div>; }
